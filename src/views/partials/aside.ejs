<aside id="sidebar" class="fixed inset-y-0 left-0 z-30 w-64 md:w-72 bg-white border-r border-gray-200 flex flex-col justify-between h-full shadow-lg md:shadow-sm transform -translate-x-full md:translate-x-0 md:relative sidebar-transition transition-transform duration-300 flex-shrink-0 overflow-y-auto no-scrollbar">
  <div>
    <div class="p-5 border-b border-gray-100">
      <div class="flex items-center gap-3">
        <div class="w-9 h-9 rounded-xl bg-brand-50 text-brand-800 flex items-center justify-center shrink-0">
          <img src="/assets/seguridad/logo-charlotte.webp" alt="Charlotte" class="w-8 h-8 object-contain" loading="eager">
        </div>
        <div class="min-w-0">
          <div class="text-sm font-extrabold text-slate-900 leading-5">Seguridad</div>
          <div class="text-xs text-slate-500 leading-4 truncate">Panel Administrativo</div>
        </div>
        <!-- Botón de cierre móvil opcional -->
        <button type="button" class="md:hidden ml-auto text-gray-400 hover:text-gray-600" aria-label="Cerrar menú" data-sidebar-close>
          <i class="fa-solid fa-xmark"></i>
        </button>
      </div>
      <a href="/admin" class="mt-3 inline-flex items-center gap-2 text-xs text-gray-500 hover:text-gray-800">
        <span aria-hidden="true">←</span>
        <span>Volver a Admin</span>
      </a>
    </div>

    <nav class="px-3 pb-4 space-y-2 mt-2">
      <a href="/seguridad/usuarios" class="group flex items-start gap-3 rounded-xl px-3 py-2 transition-colors border text-slate-700 hover:bg-slate-50 border-transparent">
        <div class="mt-0.5 nav-icon text-slate-500 group-hover:text-slate-700"><i class="fa-solid fa-user-group"></i></div>
        <div class="min-w-0">
          <div class="text-sm font-semibold leading-5">Usuarios</div>
          <div class="text-xs text-gray-500 leading-4 truncate">Gestión de usuarios</div>
        </div>
      </a>

      <a href="/seguridad/roles" class="group flex items-start gap-3 rounded-xl px-3 py-2 transition-colors border text-slate-700 hover:bg-slate-50 border-transparent">
        <div class="mt-0.5 nav-icon text-slate-500 group-hover:text-slate-700"><i class="fa-solid fa-user-shield"></i></div>
        <div class="min-w-0">
          <div class="text-sm font-semibold leading-5">Roles</div>
          <div class="text-xs text-gray-500 leading-4 truncate">Permisos por rol</div>
        </div>
      </a>

      <a href="/seguridad/permisos" class="group flex items-start gap-3 rounded-xl px-3 py-2 transition-colors border text-slate-700 hover:bg-slate-50 border-transparent">
        <div class="mt-0.5 nav-icon text-slate-500 group-hover:text-slate-700"><i class="fa-solid fa-key"></i></div>
        <div class="min-w-0">
          <div class="text-sm font-semibold leading-5">Permisos</div>
          <div class="text-xs text-gray-500 leading-4 truncate">Matrices de acceso</div>
        </div>
      </a>

      <a href="/seguridad/restaurante/coordenadas" class="group flex items-start gap-3 rounded-xl px-3 py-2 transition-colors border text-slate-700 hover:bg-slate-50 border-transparent">
        <div class="mt-0.5 nav-icon text-slate-500 group-hover:text-slate-700"><i class="fa-solid fa-location-dot"></i></div>
        <div class="min-w-0">
          <div class="text-sm font-semibold leading-5">Restaurante Coordenadas</div>
          <div class="text-xs text-gray-500 leading-4 truncate">Geolocalización</div>
        </div>
      </a>

      <a href="/seguridad/perfil/editar" class="group flex items-start gap-3 rounded-xl px-3 py-2 transition-colors border text-slate-700 hover:bg-slate-50 border-transparent">
        <div class="mt-0.5 nav-icon text-slate-500 group-hover:text-slate-700"><i class="fa-solid fa-gear"></i></div>
        <div class="min-w-0">
          <div class="text-sm font-semibold leading-5">Mi perfil</div>
          <div class="text-xs text-gray-500 leading-4 truncate">Preferencias</div>
        </div>
      </a>
    </nav>
  </div>

  <div class="p-4 border-t border-gray-100">
    <% if (user) { %>
      <a href="/seguridad/logout" class="flex items-center gap-3 text-red-500 hover:bg-red-50 px-5 py-3 rounded-xl w-full transition-colors text-base font-semibold">
        <i class="fa-solid fa-right-from-bracket"></i>
        <span>Cerrar Sesión</span>
      </a>
    <% } %>
  </div>
</aside>

<!-- Overlay y botón de apertura para móviles -->
<div id="sidebar-overlay" class="fixed inset-0 bg-black/40 z-20 md:hidden hidden"></div>
<button id="sidebar-open" type="button" class="fixed bottom-4 right-4 z-40 md:hidden rounded-xl bg-white border border-gray-200 shadow px-3 py-2 flex items-center gap-2 text-slate-700">
  <i class="fa-solid fa-bars"></i>
  <span class="text-sm font-semibold">Menú</span>
</button>

<script>
  (function() {
    const sidebar = document.getElementById('sidebar');
    const overlay = document.getElementById('sidebar-overlay');
    const btnOpen = document.getElementById('sidebar-open');
    const btnClose = sidebar ? sidebar.querySelector('[data-sidebar-close]') : null;

    function openSidebar() {
      if (!sidebar) return;
      sidebar.classList.remove('-translate-x-full');
      sidebar.classList.add('translate-x-0');
      if (overlay) overlay.classList.remove('hidden');
    }

    function closeSidebar() {
      if (!sidebar) return;
      sidebar.classList.add('-translate-x-full');
      sidebar.classList.remove('translate-x-0');
      if (overlay) overlay.classList.add('hidden');
    }

    if (btnOpen) btnOpen.addEventListener('click', openSidebar);
    if (btnClose) btnClose.addEventListener('click', closeSidebar);
    if (overlay) overlay.addEventListener('click', closeSidebar);

    // Cierra el sidebar al navegar por enlaces en móvil para evitar estados superpuestos
    if (sidebar) {
      sidebar.addEventListener('click', function(e) {
        const a = e.target.closest('a');
        if (a && window.matchMedia('(max-width: 767px)').matches) {
          // Pequeño retraso para permitir la navegación
          setTimeout(closeSidebar, 150);
        }
      });
    }
  })();
</script>
